CREATE TABLE users(id SERIAL PRIMARY KEY, name VARCHAR(20), surname VARCHAR(20),  handle VARCHAR(60), email VARCHAR(60), password VARCHAR(60), date_created TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);
